/** 
  Atributos (Dados):
    marca: A marca do celular (string).
    modelo: O modelo do celular (string).
    cor: A cor do celular (string).
    bateria: O nível da bateria (número, de 0 a 100).
    ligado: Indica se o celular está ligado ou desligado (boolean).
    camara: A camera que será usada para tirar foto.
  Métodos:
    ligar(): Liga o celular, se tiver bateria.
    desligar(): Desliga o celular.
    fazerChamada(numero: string): Simula a realização de uma chamada telefônica, consumindo bateria.
    carregar(quantidade: number): Aumenta o nível da bateria.
    tirarFoto(): Simula a ação de tirar uma foto, chamando a camera e diminuindo a bateria.
    status(): Exibe o status atual do celular, como o nível de bateria e se está ligado ou desligado.
*/

//Bateria
//Camera